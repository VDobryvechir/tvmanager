(function(){
  const picWidth = 1280;
  const picHeight = 800;
  const primColor = '#fff8dc';
  const secColor = '#';
  function analyzeText(txt, wid, hei, canv) {
    const res = [];
    while(txt) {
        
    }
  } 
  function meldText(mes,url) {
  }
  window.meldText=meldText;
}())